# Advanced Usage
# You don't need to edit this file unless you are an advanced user.

ifeq ($(ARTIFACT_BASE_NAME),)
	ARTIFACT_BASE_NAME = [% project.package_name %]
endif

ifeq ($(ARTIFACT_NAME),)
	ARTIFACT_NAME = [% project.package_name %]
endif

